import numpy as np
import scipy as scp

class medias():

    def __init__(self):
        self.data_array = []
        self.data = int(input('numero de valores: '))
        for a in range(sel.data):
            self.value = float(input('valor: '))
            self.data_array.append(self.value)

    def promedio(self):
        self.temp = 0
        for b in self.data_array:
            self.actualv = self.dataarray[b]
            self.temp = self.temp + self.actualv
        self.promedio = self.temp

    def Z_table(self):
